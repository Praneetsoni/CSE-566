Reporting Non-redundant Sequences
----------------------------------

The code implemented in this project is in the path "rlcsa/main.cpp".
 
Makefile has been modified to compile it with the other binaries.

All the datasets as well as the data generated by this code is dumped in the /data folder.

Command Line 
------------

./main data/<dataset_name>

